# Дорожная карта проекта

Единственный источник правды о том, что сделано, что делается и почему приняты
те или иные решения. Вкладки Claude Code не синхронизируются между собой —
общее у них только содержимое репозитория, поэтому актуальное состояние живёт
здесь, а не в истории переписки.

**Файл обязателен к обновлению.** Окно, закрывшее задачу, дописывает сюда
результат до того, как задача считается завершённой.

---

## Как продолжать работу

1. Задачи берутся по одной, в порядке приоритета из списка ниже.
2. Под задачу заводится ветка от `dev`: `git checkout -b <тип>/<имя> dev`.
3. Задача решается в отдельной вкладке. Ввод в курс дела — одной фразой:
   «прочитай `docs/ROADMAP.md`, берём задачу N».
   Мелкие задачи (десять минут, пара команд) выгоднее добивать в текущей
   вкладке: новое окно потратит больше времени на разведку, чем сама задача.
4. Обсуждение предшествует изменениям. Код и конфигурация правятся после
   явного согласия.
5. Коммиты и описания pull request — подробные, по-русски: что сделано,
   зачем и что это меняет.
6. По завершении задача дописывает в этот файл: что сделано, какие решения
   приняты по ходу, что осталось незакрытым.

Несколько вкладок делят одну рабочую копию и одну текущую ветку, поэтому
задачи идут последовательно. Для параллельной работы нужен отдельный
git worktree.

---

## Состояние на 7 сентября 2026

**Ветки.** `main` и `dev` сведены на коммите `12e260e`, расхождение нулевое,
содержимое идентичное. Все 16 веток влиты в `main`. Четырнадцать веток
слитых фич не удалены — см. задачу 6.

**Продакшен.** `https://art-jane.vercel.app`, проект Vercel
`serhiis-projects-1baa2745/art-jane`, тариф Hobby. Последний деплой `Ready`.
Авария авторизации, из-за которой не работали ни вход по паролю, ни Google,
устранена: прод крутил код из старого `main`, где в схеме Prisma ещё
существовали `User.address` и `Product.orderId`, тогда как общая база уже
ушла вперёд — каждое обращение к `user` и `Product` падало с `ColumnNotFound`.

**Что уже сделано в платёжном флоу** (пять задач предыдущего этапа):

- проверка владельца заказа перенесена вперёд мутации на странице успеха,
  логика вынесена в `src/lib/checkout-outcome.ts`;
- `createCheckoutSession` сериализован мьютексом `CheckoutLock` с TTL 90 секунд;
- история миграций Prisma приведена в соответствие с реальной схемой;
- юнит-тесты (33 штуки), смок-тесты Playwright и работающий CI;
- ресинк Zustand-стора и явная обработка сбоя базы на странице оформления.

**Переменные окружения на Vercel** (проверено `vercel env ls`):

| Переменная                    | Окружения                | Замечание                      |
| ----------------------------- | ------------------------ | ------------------------------ |
| `STRIPE_SECRET_KEY`           | Production               | Починен 7 сентября, проверен   |
| `STRIPE_WEBHOOK_SECRET`       | Production               | Добавлен 7 сентября, проверен  |
| `CRON_SECRET`                 | Production               | В порядке                      |
| `NEXT_PUBLIC_APP_URL`         | Preview, Production      | В порядке                      |
| `NEXT_PUBLIC_BETTER_AUTH_URL` | Production, Preview      | В коде не используется         |
| `BETTER_AUTH_URL`             | Dev, Preview, Production | Одно значение на все окружения |
| `DATABASE_URL` / `DIRECT_URL` | Dev, Preview, Production | Одна база на всё               |
| `NEXT_PUBLIC_SUPABASE_*`      | Dev, Preview, Production | В порядке                      |

`STRIPE_PUBLISHABLE_KEY` лежит в локальном `.env`, но в коде не встречается
ни разу — оформление заказа построено на редиректе, ключ не нужен. На Vercel
его добавлять не следует.

---

## Задача 0 — битый `STRIPE_SECRET_KEY` в продакшене

**Статус:** закрыта 7 сентября 2026. Ключ перезаписан валидным значением из
локального `.env` командой `vercel env add … --sensitive --force` (ответ CLI —
«Overrode»), продакшен передеплоен. После этого `[CANCEL_API]` и
`[CHECKOUT_SUCCESS]` перестали писать ошибку, а приложение снова создаёт
сессии Stripe — проверено настоящим оформлением заказа.

**Что произошло.** Значение `STRIPE_SECRET_KEY` в окружении Production — это
буквально строка `sk_test_` длиной восемь символов, то есть обрезанная при
вставке. Stripe отвечает на неё `StripeAuthenticationError: Invalid API Key
provided: sk_test_`.

**Как это доказано, а не предположено.** Прод нельзя расспросить напрямую:
Vercel не отдаёт значения Secret-переменных, `vercel env pull` возвращает
`[SENSITIVE]`. Поэтому диагноз собран из двух наблюдений. Первое: в
боевых логах ошибка воспроизводится на двух независимых путях кода —
`[CANCEL_API]` из `src/app/api/checkout/cancel/route.ts` и
`[CHECKOUT_SUCCESS]` из `src/lib/checkout-outcome.ts`, оба через
`stripe.checkout.sessions.retrieve`. Второе: Stripe маскирует ключ в этом
сообщении по понятному правилу — ключ до двенадцати символов возвращается
как есть, длиннее заменяется звёздочками с сохранением последних четырёх
(проверено тремя заведомо неверными ключами). Прод отдаёт `sk_test_` без
единой звёздочки, а значит настоящее значение и есть эти восемь символов.

**Чем это грозит прямо сейчас.** Продакшен вообще не может разговаривать с
API Stripe. Оформление заказа падает на `stripe.checkout.sessions.create`;
по ветке в `src/actions/checkout.ts` это не сетевая ошибка, поэтому бронь
откатывается, заказ уходит в `CANCELLED`, а покупатель видит «Ошибка связи с
платежной системой. Попробуйте еще раз». Данные при этом не портятся — откат
отрабатывает штатно, — но купить в магазине нельзя ничего. Судя по метке
создания переменной, так продолжается около суток.

**Чем чинится.** Ключ из локального `.env` проверен и валиден: 107 символов,
тестовый режим, аккаунт `acct_1TnJtKL9uPyB4436`, успешно достаёт сессию через
API. Его достаточно переложить в Vercel, не печатая значение в терминал:

```
vercel env rm STRIPE_SECRET_KEY production
grep -E '^STRIPE_SECRET_KEY=' .env | cut -d= -f2- | tr -d '"' \
  | vercel env add STRIPE_SECRET_KEY production --sensitive
```

После этого обязателен редеплой — переменные подхватываются только новой
сборкой.

---

## Задача 1 — вебхук Stripe в продакшене

**Статус:** закрыта 7 сентября 2026. Вебхук работает в продакшене, проверен
сквозными сценариями с настоящей оплатой. Ветка
`feat/stripe-production-webhook`, коммиты `fe197c8` и `3117131`.

**Проблема (исходная).** `STRIPE_WEBHOOK_SECRET` отсутствовал на Vercel, а
`src/app/api/webhooks/stripe/route.ts` читает его на уровне модуля и передаёт
в `constructEventAsync`. Даже если создать эндпоинт в дашборде Stripe, каждое
событие упадёт на верификации подписи и вернёт 400. Локальный секрет из
`stripe listen` для продакшена не подойдёт — у постоянного эндпоинта свой.

**Чем это грозит.** Сейчас единственный путь проведения заказа — страница
успеха (`resolveOutcome` вызывает `fulfillOrder`). Заказ проводится, только
если покупатель дождался редиректа из Stripe обратно. Если он закрыл вкладку
сразу после оплаты, заказ остаётся `PENDING`; через 40 минут
(`DB_RESERVATION_MINUTES` в `src/actions/checkout.ts`) он попадает под снятие
брони. Итог: деньги списаны, вещь снова в продаже, заказа нет. Вебхук
задумывался именно как страховка от этого и не работает.

**Сделано.**

1. Эндпоинт заведён через Stripe CLI: `we_1UCydkL9uPyB44368SAnNf3h`,
   `https://art-jane.vercel.app/api/webhooks/stripe`, статус `enabled`,
   тестовый режим. Подписан на четыре события: `checkout.session.completed`,
   `checkout.session.async_payment_succeeded`,
   `checkout.session.async_payment_failed`, `checkout.session.expired`.
   Постоянного эндпоинта до этого не существовало вовсе — не только секрета
   не было, но и адресата, так что Stripe никуда ничего не слал.
   `api_version` эндпоинта прибит к `2026-06-24.dahlia`, той же, что
   `STRIPE_API_VERSION` в `src/lib/stripe.ts`: иначе смена версии по умолчанию
   у аккаунта тихо разошлась бы с типами SDK.
2. Секрет добавлен в Vercel как Secret для Production.
3. Код: в `src/app/api/webhooks/stripe/route.ts` секрет читается внутри
   обработчика, без non-null assertion, отсутствие даёт 500 и отдельный лог.
   Комментарий в `src/app/api/cron/release-reservations/route.ts` приведён
   к реальному суточному расписанию.

**Как проверено.** Замер «до»: сразу после создания эндпоинта `stripe trigger`
дошёл до прода и получил `400` с `Received undefined` в логах, Stripe ретраил
трижды — доставка работает, причина отказа именно пустой секрет, а старый код
и правда маскировал конфигурационную ошибку под подделку подписи. Замер
«после» и два сквозных сценария описаны в журнале.

---

## Задача 2 — переключение на боевые ключи Stripe

**Статус:** не начата. Делать последней.

Продакшен работает на тестовом ключе `sk_test_`, реальные платежи не
принимаются. У боевого режима отдельный вебхук-эндпоинт со своим секретом,
поэтому `STRIPE_SECRET_KEY` и `STRIPE_WEBHOOK_SECRET` переключаются вместе.

Переключаться имеет смысл только после ручной проверки сценариев из задачи 5.

---

## Задача 3 — гигиена окружения

**Статус:** не начата. Мелкая, делается в текущей вкладке пачкой.

- Добавить `STRIPE_SECRET_KEY` в Preview. Сейчас preview-сборки падают на
  `Neither apiKey nor config.authenticator provided` при вычислении модуля
  `src/lib/stripe.ts`, из-за чего проверки в pull request бесполезны.
- Удалить `NEXT_PUBLIC_BETTER_AUTH_URL` — мёртвая переменная.
- Развести `BETTER_AUTH_URL` по окружениям. В `src/lib/auth.ts` не задан
  `baseURL`, better-auth берёт его из этой переменной, а она одна на все
  окружения. На preview-деплое `baseURL` указывает на прод, `trustedOrigins`
  выводится из него — авторизация на превью не работает.

---

## Задача 4 — разделить базы разработки и продакшена

**Статус:** не начата. Крупная, отдельная вкладка.

Сейчас `DATABASE_URL` и `DIRECT_URL` — один Config-набор на Development,
Preview и Production, то есть локальная разработка и прод ходят в **одну и ту
же** базу Supabase. Именно это превратило обычное отставание деплоя в аварию
авторизации.

Риск не разовый: любой локальный `prisma db push`, `prisma:seed` или тест
пишет в боевые данные. Починку истории миграций уже пришлось делать на живой
базе с 19 заказами.

Нужно: завести отдельный проект Supabase для разработки, сделать
`DATABASE_URL`/`DIRECT_URL` пер-окруженческими, перевести локальный `.env`
на базу разработки.

---

## Задача 5 — cron и ручная проверка платежей

**Статус:** не начата. Отдельная вкладка.

Расписание в `vercel.json` — `0 3 * * *`, суточное, из-за ограничений тарифа
Hobby. При TTL брони в 40 минут брошенная бронь держит уникальную вещь до
суток. Роут спроектирован так, что его может дёргать любой внешний
планировщик с заголовком `Authorization: Bearer $CRON_SECRET`, поэтому
расписание в GitHub Actions решает задачу бесплатно. Альтернативы —
Supabase `pg_cron` или тариф Vercel Pro.

**Доказательство, что cron ни разу не отработал.** На 7 сентября в базе висит
заказ `6fc7316b-500f-4bc8-8603-ed3ec260993c` в статусе `PENDING` с
`expiresAt` = 1 августа 2026, и три товара (`…-27`, `…-38`, `…-40`) под бронью
с тем же протухшим `reservedUntil`. Это ровно та выборка, которую разбирает
`releaseExpiredReservations`: статус `PENDING`, срок в прошлом, `reservedUntil`
совпадает с `expiresAt` заказа, то есть воркер снял бы бронь без всяких помех.
Пять недель товары числятся занятыми — значит, роут не вызывался ни разу
успешно. Первым делом при взятии задачи стоит дёрнуть его руками
(`Authorization: Bearer $CRON_SECRET`) и посмотреть, отработает ли он вообще,
и только потом разбираться с расписанием.

Сценарии, оставшиеся непройденными:

- две вкладки одновременно на одном аккаунте — ожидается `PROCESSING_ERROR`,
  а не отмена первого заказа;
- открытие чужого `session_id` под другим пользователем — ожидается
  `UnconfirmedView` без мутации заказа. Косвенно подтверждено 7 сентября:
  разлогиненный посетитель на странице успеха получил `UnconfirmedView`, и
  заказ мутирован не был. Отдельная проверка «чужой залогиненный
  пользователь» всё ещё не проводилась.

Реальный платёж тестовой картой от корзины до конца **проверен** 7 сентября
в рамках задачи 1 — подробности в журнале.

---

## Задача 6 — отложенное

**Статус:** не начата. Крупная часть — отдельная вкладка.

- **Варианты и размеры товара.** Обсуждалось на примере «десять белых футболок
  в трёх размерах». Текущая модель исходит из того, что каждая вещь уникальна
  и существует в единственном экземпляре. Решение отложено осознанно, чтобы не
  блокировать платёжный флоу.
- Удалить 14 влитых веток фич.
- Запросы вида `WHERE "Product"."id" IN (NULL)` при пустом массиве.

---

## Журнал выполнения

Сюда каждое окно дописывает результат закрытой задачи.

- **7 сентября 2026.** Сведены `main` и `dev` на `12e260e`. Проведён аудит
  окружения Vercel, составлен этот план, зафиксирован порядок работы.
- **7 сентября 2026, задачи 0 и 1 — закрыты.** Заведён вебхук-эндпоинт
  `we_1UCydkL9uPyB44368SAnNf3h`, секрет положен в Vercel, код поправлен
  (`fe197c8`). По ходу проверки обнаружена задача 0 — битый
  `STRIPE_SECRET_KEY`; ключ перезаписан валидным значением, продакшен
  передеплоен, после чего проведена полная проверка.

    **Три шага, каждый доказывает ровно одну вещь.**

    1. _Подпись сходится._ `stripe trigger checkout.session.completed` →
       `POST /api/webhooks/stripe -> 200` и лог «В metadata сессии … нет
       orderId»: сессию создал триггер, `orderId` в ней и не должно быть.
       До редеплоя тот же запрос давал 400.
    2. _Снятие брони без оплаты._ Через интерфейс прода оформлен настоящий заказ
       `31c8ffb6` (товар `distressed-t-shirt-guardian-cat-39`, 125 €). Состояние
       до: заказ `PENDING`, товар `RESERVED`, `reservedUntil` совпадает с
       `expiresAt`. Браузер уведён на `about:blank`, затем
       `stripe checkout sessions expire`. Через десять секунд: заказ
       `CANCELLED`, товар `AVAILABLE`, `reservedUntil` пуст, две доставки вебхука
       с кодом 200. Ни одна страница приложения не была открыта, cron спит до
       трёх ночи, cancel-роут не дёргался — сработать мог только вебхук.
    3. _Оплата, когда покупатель не дошёл до страницы успеха._ Оформлен заказ
       `95bd8c0c`, затем во второй вкладке выполнен выход из аккаунта, и только
       после этого оплата картой `4242 4242 4242 4242`. Страница успеха показала
       «НЕ УДАЛОСЬ ПОДТВЕРДИТЬ ЗАКАЗ» — проверка владельца отсекла разлогиненного
       посетителя и мутации не сделала, как и задумано в `checkout-outcome.ts`.
       Тем не менее заказ стал `PAID`, товар — `SOLD`, позиции удалены из корзины
       в базе, вебхук ответил 200, `[ALARM_ORPHAN_PAYMENT]` в логах нет. Провести
       заказ мог только вебхук.

    Приём с выходом из аккаунта надёжнее, чем гонка за скоростью закрытия
    вкладки, и воспроизводится с первого раза — стоит пользоваться им и дальше.

    **Совпадение аккаунтов Stripe подтверждено.** Локальный Stripe CLI успешно
    достал через API сессию, **созданную продакшеном**. Это снимает оговорку,
    которую нельзя было закрыть раньше: Vercel не отдаёт значения
    Secret-переменных, а верификация подписи вебхука API-ключ не использует,
    поэтому дымовой тест подписи сам по себе совпадения аккаунтов не доказывал.

    **Уборка.** Тестовый аккаунт `claude.webhook.test@example.com`, оба заказа и
    их позиции удалены, товар возвращён в `AVAILABLE`. База сверена со снимком
    «до» и совпала цифра в цифру: 19 заказов (`PENDING`=1, `CANCELLED`=17,
    `PAID`=1), товары `SOLD`=1, `AVAILABLE`=37, `RESERVED`=3, 5 пользователей.

    **Ловушки, выясненные по ходу.** Все тестовые заказы ложатся в боевую базу
    Supabase — она одна на все окружения (задача 4). Пока запущен
    `stripe listen`, он получает все события тестового режима аккаунта, включая
    порождённые продом, и при общей базе локальный обработчик начнёт менять те же
    самые заказы: во время прод-проверок его надо держать выключенным. Локальному
    приложению нужен свой `whsec_…` из вывода `stripe listen`, с продовым он не
    связан. В `src/actions/checkout.ts` задано `payment_method_types: ['card']`,
    поэтому `checkout.session.async_payment_succeeded` и `async_payment_failed`
    в проде не выстрелят никогда — они подписаны на будущее, когда появятся SEPA
    или Klarna, и их молчание не признак поломки.

    **Осталось незакрытым.** В `.claude/settings.local.json` (не в git) лежит
    разрешение на команды `vercel` — оно понадобится задачам 2, 3 и 5; когда
    работа с окружением закончится, файл нужно удалить. Ветка
    `feat/stripe-production-webhook` не влита: нужен pull request в `dev`, затем
    в `main`. Правка вебхука, различающая отсутствие секрета, поедет в прод
    вместе с этим PR — сейчас в проде код из `main`.
